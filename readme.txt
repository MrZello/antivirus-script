Gist:
	Architecture
		Agent
		Server
		Virustotal API
	Must only submit new files (never before seen on your system) to VT
            Baseline your folder first, then...
            Sumbit only files that are seen after the baseline
	Rescans folder every 5 minutes
	Immediately notify the user if a malicious file is found
	Malicious is indicated by any result above 4 hits OR 5% of engines on VT
	Then notifies the user after the check is complete 

https://developers.virustotal.com/reference


